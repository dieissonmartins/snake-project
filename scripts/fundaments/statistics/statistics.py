import statistics

qtd_peoples = [1, 2, 3, 4]

# average values
avg = statistics.mean(qtd_peoples)
print(avg)

# median values
median = statistics.median(qtd_peoples)
print(median)

# mode values
mode = statistics.mode(qtd_peoples)
print(mode)
